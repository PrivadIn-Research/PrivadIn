{
  "name": "privadin-functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0",
    "privadin": "file:.."
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": "22"
  }
}
